<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <!-- 1. Создаем последовательности для генерации ID -->
    <changeSet id="1-create-sequences" author="brazhalovich">
        <createSequence sequenceName="user_seq" incrementBy="1" startValue="1"/>
        <createSequence sequenceName="news_seq" incrementBy="1" startValue="1"/>
        <createSequence sequenceName="marker_seq" incrementBy="1" startValue="1"/>
        <createSequence sequenceName="comment_seq" incrementBy="1" startValue="1"/>
    </changeSet>

    <!-- 2. Создаем таблицу User (tbl_user) -->
    <changeSet id="2-create-tbl-user" author="brazhalovich">
        <createTable tableName="tbl_user">
            <column name="id" type="BIGINT" defaultValueComputed="nextval('user_seq')">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="login" type="VARCHAR(64)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="password" type="VARCHAR(128)">
                <constraints nullable="false"/>
            </column>
            <column name="firstname" type="VARCHAR(64)"/>
            <column name="lastname" type="VARCHAR(64)"/>
        </createTable>
    </changeSet>

    <!-- 3. Создаем таблицу News (tbl_news) -->
    <changeSet id="3-create-tbl-news" author="brazhalovich">
        <createTable tableName="tbl_news">
            <column name="id" type="BIGINT" defaultValueComputed="nextval('news_seq')">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_news_user" references="tbl_user(id)"/>
            </column>
            <column name="title" type="VARCHAR(64)">
                <constraints nullable="false" unique="true"/> <!-- Добавь unique="true" -->
            </column>
            <column name="content" type="VARCHAR(2048)">
                <constraints nullable="false"/>
            </column>
            <column name="created" type="TIMESTAMP"/>
            <column name="modified" type="TIMESTAMP"/>

        </createTable>
    </changeSet>

    <!-- 4. Создаем таблицу Marker (tbl_marker) -->
    <changeSet id="4-create-tbl-marker" author="brazhalovich">
        <createTable tableName="tbl_marker">
            <column name="id" type="BIGINT" defaultValueComputed="nextval('marker_seq')">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="name" type="VARCHAR(32)">
                <constraints nullable="false" unique="true"/>
            </column>
        </createTable>
    </changeSet>

    <!-- 5. Создаем таблицу Comment (tbl_comment) -->
    <changeSet id="5-create-tbl-comment" author="brazhalovich">
        <createTable tableName="tbl_comment">
            <column name="id" type="BIGINT" defaultValueComputed="nextval('comment_seq')">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="news_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_comment_news" references="tbl_news(id)"/>
            </column>
            <column name="content" type="VARCHAR(2048)">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- 6. Создаем связующую таблицу для News и Marker (Many-to-Many) -->
    <changeSet id="6-create-tbl-news-marker" author="brazhalovich">
        <createTable tableName="tbl_news_marker">
            <column name="news_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_nm_news" references="tbl_news(id)"/>
            </column>
            <column name="marker_id" type="BIGINT">
                <constraints nullable="false" foreignKeyName="fk_nm_marker" references="tbl_marker(id)"/>
            </column>
        </createTable>
        <addPrimaryKey tableName="tbl_news_marker" columnNames="news_id, marker_id"/>
    </changeSet>

    <!-- 7. Вставка начального пользователя (согласно заданию) -->
    <changeSet id="7-insert-initial-user" author="brazhalovich">
        <insert tableName="tbl_user">
            <column name="id" valueNumeric="1"/> <!-- ФИКСИРОВАННЫЙ ID -->
            <column name="login" value="sashabrazhalovich2005@gmail.com"/>
            <column name="password" value="temp_password"/>
            <column name="firstname" value="Александр"/>
            <column name="lastname" value="Бражалович"/>
        </insert>
        <!-- Сбрасываем счетчик, чтобы следующие ID начинались после 1 -->
        <sql>SELECT setval('user_seq', 1);</sql>
    </changeSet>

</databaseChangeLog>